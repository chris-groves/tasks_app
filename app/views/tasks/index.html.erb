<% require './lib/feature' %>

<h1>Tasks</h1>

<ul>
  <% @tasks.each do |task| %>
    <li>
      <a href="/tasks/<%= task.id %>">
        <%= task.description %>
    </li>
  <% end %>
</ul>


<%= link_to 'Add New Task', tasks_new_path %>
